<% provide(:title, 'New Contribution') %>

<div class="col-md-offset-3 col-sm-6">

<% if @contribution.errors.any? %>
	<% @contribution.errors.full_messages.each do |msg| %>
	<li><%= msg %></li>
	<% end %>
<% end %>

<h2>New Contribution</h2>
<p>Enter a new political contribution in the form below.</p>

<%= render partial: 'form' %>

</div>