<% provide(:title, 'Edit Contribution') %>

<div class="col-md-offset-3 col-sm-6">

<% if @contribution.errors.any? %>
	<% @contribution.errors.full_messages.each do |msg| %>
	<li><%= msg %></li>
	<% end %>
<% end %>

<h2>Edit Contribution</h2>
<p>Edit your political contribution in below.</p>

<%= render partial: 'form' %>

</div>